# HANDOVER — 19 Aug 2026 — THE WRITER: the mark leads, the cite guard, Auto cite with websites, whiteboard → page, the tour + the Introduction project, the brainstorm

*Read `CLAUDE.md` (the operating contract) first. Then `docs/HANDOVER_2026-08-18_WRITER-Q-IS-THE-COACH.md` §0 ("how to be me") — still every line. Then `docs/HANDOVER_2026-08-18_KITE_WRITER-MARKER-STANDARD-AND-LABELS.md`. Then this. Every claim below is in a commit on `main` (`072f005` … `e31fdf6`, all pushed, all live) or in Sarah's words. I was the lead on the writer for this session; she asked for one agent as single point of contact — be that.*

---

## 0. START HERE — the two open things, then the test

1. **The Introduction project's brief + mark are NOT generated yet.** `plugins/writer-demo-template.json` does not exist. Sarah has not yet said yes to the ~15–20p it costs (one brief read + one Mark & fix, once). When she does: `node scripts/build-writer-demo.js` (reads `.env.local`), check the output (grade, per-part labels), commit `plugins/writer-demo-template.json` by path, push. Until then the demo runs without a brief/mark — the tour's last step rings Mark & fix instead of opening the Marks panel on a real mark. **Never hand-write that mark.** (Rule: no paid tests without £ + yes — see memory `feedback_no_paid_tests_without_price_and_yes`.)
2. **Word download (`/writer/download` → `createDocx`) is plain text** — tables, diagrams, mind maps on the page do not reach the .docx. Said to Sarah; not built.
3. **The test she wants:** re-mark the 7HR02 essay (CIPD L7, real Distinctions) and compare the marker's LO marks and reasons to hers; and run Q on the Claude brain vs V4 for the cost comparison (`GET /writer/coachlog`). Neither started.

---

## 1. WHAT SHIPPED TODAY (all on `main`, all live) — newest last

| commit | what |
|---|---|
| `072f005` | **The mark leads the panel**: THE MARK block first — a grade tile per question (press → its card), the overall big with "11 of 16 · 4 of 4 answered · scheme", then **"N fixes to a <next grade>"** (press → the ladder); LO row + one derived line; ladder; question cards; reading tiles after. **`mark-schemes.js` `cipdL7DeriveLoMarks`**: when the model returns `loMarks: []` (her live mark did), derive them from the question grades (AC n.x → LO n; several in one LO → mean, half rounds DOWN; missing → 1; C1/C2 → one each), reason says derived; schema + standard block say loMarks REQUIRED. |
| `6ddd3bc` → `469d357` | THE IDEA / DO THIS inside a what-to-add-next item: she said cards were messy — now flat, a small heading each, one hairline between. |
| `9af6d17` | **`critIdResolver`**: a graded question is never "not started" for its id spelling (exact → letters/digits → ordinal "1"/"Q1"/"part 1" → label → position); unmatched ids logged. Band words ("Mid"/"Top") are never shown as grades; junk `toNext` dropped. **Scenario is never the student or their draft** (brief schema + `extractScenario`) — the law brief file said "her draft notes say…" and the reader built a case study out of it (PROS/CONS were about her essay). Empty plans log what came back. |
| `814af2d` | **THE CITE GUARD**: every citation-shaped mention in Q's reply/whiteboard (`q-cite.js findMentions`: Guest (1998), (Rousseau, 1995), Conway and Briner (2005), et al., DOI; stop list so "Act (1998)" isn't one) is looked up on **OpenAlex by author name + year range** (`raw_author_name.search` + publication dates; CrossRef `query.author` behind) with the sentence's topic words — NOT full-text search (author names are not in it; a first version called Guest (1998) invented). strong = topic word matches; weak = name+year only (common surname); not found → appended to HIS reply: "⚠️ I could not find **X** on OpenAlex or CrossRef…". Her own words exempt (page, message, uploads, brief, check_reference results). Log: `[writer/chat] cite guard`. Prompt rule in q-chat writer-coach: never name a source from memory. Honest limit: a real name on the wrong claim passes. |
| `4c1e721` | **Auto cite: papers AND websites** — `q-cite.js searchWebSources` (Brave, same key as Q's web_search; org from domain map or the engine's profile name; year from URL/page date/snippet else n.d.; Harvard for web in code; social/essay mills blocked). Route: 8 papers + 6 sites (was 5); judge sees up to 14 (was 6 — why she saw three). Pop: PAPERS AND BOOKS / WEBSITES, strong first, URL on each. **Reference removal** tolerant (exact → normalised → first 60 chars → title/URL under References). |
| `1ed318a` | **Tables on the page**: a display table → `Send to my page` → a real `<table class="q-table">` in the document's type (`sendBlockToPage` — after the caret's block, never inside a sentence). **Calculator + current_datetime in `WRITER_TOOLS`** (was trigger-gated on the student's words). Prompt: NUMBERS through the calculator; TABLES land as tables. |
| `400a982`→`436eb6b`, `73e644d` | **Show me around (the tour)** — scripted, Q's voice, no model call (built by an Opus sub-agent, reviewed + re-run by me): each step fires the REAL function (addQNotes, addQTabs, addStickies, showOnWhiteboard, openEditPanel, openMarkPanel), `Next →` / `Stop the tour` taps, clean-up at the end. Entry: first tap on a fresh project, else the quiet pill under Q's name. |
| `6da0043` | **Whiteboard → page, formatted**: `wbBlocksFromSrc` reads a display as blocks — prose (at the caret as words), lists (bullets kept; ticked lines only when any are ticked), tables, diagram fences (→ `.q-figure`, ruled boxes, ink arrows, not editable inside). Sticky → page always lands (caret, else END of page + ↶ Undo in the work area). Editing-panel **Auto cite from the cursor** (no highlight gate). |
| `88d4120` | **Coach card scrolls, doesn't grow** (default ceiling `min(62vh,640px)` — her grip resize wins; `renderThread`/`setCoach` scroll the BODY). Sticky editor "Send to my page" uses the same always-lands path. No dashed divider under stickies. Tour says "press the note to change words/colour". |
| `2f82d65` | **Highlight** and **Label this** are two buttons on any selection; label defaults to the colour of a highlight on those words (`noteOnText`), else offers "Highlight the words too, same colour"; `paintQNotes` splits ranges per text node around furniture and `renderQTabs` repaints — a highlight never paints the tab's own word. Ticked send keeps table + diagram. |
| `4b79a93` | **Brainstorm / mind map** (Opus sub-agent, reviewed): ```mindmap / brainstorm / map fence → `wbMindmapHtml` SVG — centre, branches all the way round (elliptical), children fanning, curved arrows with markers, callout colours or the rail palette, emoji icons, text measured with canvas, no-overlap separation pass; on the page as a `.q-figure` SVG in document ink. ```diagram fans (`A -> B, C, D`). Prompt teaches Q the syntax. Fence lifter in routes.js knows mindmap. |
| `818ef46` | **Chips**: one label on many places → `◀ SPELLING 2/7 ▶` on the TABS card, arrows step + jump; tour step shows the chips (teaching board opens, TABS card ringed). |
| `e31fdf6` | **Introduction to the Writer** — `plugins/writer-demo.js` (demo notebook: 260-word middle-grade essay on how a law is made, real UK Parliament reference on list + page, brief/mark from the template when built), `POST /writer/projects/demo` (one per person, flag `demo:true` in the index), page: Show me around on an empty non-demo project → opens/creates it → `sessionStorage q-tour-start` → reload → tour runs on the real page; steps work on the FIRST paragraph whatever the page holds. |

Also: live data edit (backup kept) — the law project's wrong scenario on live was nulled (`q-tutor-sarah--proj-pe153088f5e.json`, `.bak-scenario-19aug` beside it).

---

## 2. HOW TO WORK HERE (on top of the two previous handovers)

- **Read the actual state, not the panel.** Every diagnosis today came from the live tutor file (`railway ssh "node -e …"` on `/data/q-memory/q-tutor-sarah--proj-<id>.json`), the live log (`railway logs <deploymentId> -n 3000`, the app logs little), or a headless render. The "not started ×6" bug was the brief ids `C1…C6` vs the model's own — visible only in the file.
- **Heredocs eat backslashes — every single time.** Write patch scripts with the Write tool as `.py` (exact-string replace, preserve CRLF) and run them. `writer.html`, `routes.js` are CRLF; `q-cite.js`, `mark-schemes.js` LF. Apostrophes inside a JS single-quoted string inside a Python triple-quoted string: use "you are" not "you're".
- **Commit BY PATH; routes.js BY HUNK** (another chat's `/dance` hunk lives at ~line 428 unstaged; `assets/study-fx.js`, `cost-tracker.js`, `revise.html`, `server/index.js` are theirs). `scratchpad/stage-routes.py` pattern: filtered `git diff` → `git apply --cached --recount`; edit its keep-condition per commit. `git stash push --keep-index -- routes.js; node -c routes.js; git stash pop` checks the staged version.
- **Push = Railway deploy = ~40s down**; poll `https://www.quotem-ai.co.uk/health?x=n` for `uptimeSec` reset; restart local after (`node scripts/local.js` → :8090; kill the pid on :8090 first). Do not push while she is mid-mark.
- **Sub-agents (Opus) for a bounded build** in `isolation: 'worktree'`; the brief names the real functions to read, the style law, the harness pattern, "commit by path, don't push"; then cherry-pick, resolve, re-run THEIR harness against main yourself, look at the PNGs, fix the nit (there was always one: a doubled pill, a panel key `board` that is really `teach`). Worktree dirs under `.claude/worktrees/` may not delete (OneDrive lock) — `git worktree prune` later.
- **Harness pattern** (scratchpad, recreate): puppeteer at `../Quoteapp/server/node_modules/puppeteer`; intercept `http://qtest.local/writer` → `writer.html`; `GET /writer/tutor` → `.local-data/q-memory/q-tutor-sarah.json` mutated; other `/writer/*` → `{ok:true}`; drive `window.__qwriterTest` (now also exposes `runAutoCite, appendReference, noteReference, wbTablesFromSrc, tableElementFrom, sendBlockToPage, wbSendText, wbBlocksFromSrc, sendBlocksToPage, sendStickyToPage, blockElement, startTour, tourStep, stopTour, maybeOfferTour`). For the demo project: a throwaway server (`tests/writer-projects.smoke.js` pattern — spawn `server/index.js` with a temp volume, read the bootstrap password from its log, `/login`, cookie into puppeteer). Verify list: inline `<script>` parse via `new Function`, `node -c`, `node tests/writer-projects.smoke.js`, screenshot LOOKED AT.
- **Paid tests need £ + yes.** The demo template build is waiting on that.
- `q-writer.js` now exports `normaliseMark, critIdResolver` for harnesses.

---

## 3. SARAH TODAY (19 Aug)

She asked me to be the new lead ("we are so close to a working product"), then live-tested all day on live + local, one defect or one spec per message, often a screenshot: the mark buried in the panel; "these are both important… separately together" (then "no this is messy… a heading and a divider"); the law essay "not started"; "pros and cons are for the brief"; "my sister said Q's making docs up"; Auto cite "more than 3 choices… urls… remove a ref isn't taking it off"; the demo — "a pre saved doc… it didn't demonstrate"; "the chat gets bigger instead of the writing moving up"; stickies/lists/diagram onto the page; the chips with arrows; "auto cite should work from the cursor". And the idea that lit her up: **the lab** — science homework, a lab table slides up, test tubes, the kid mixes, the AI knows what happens and makes it happen ("we are building this by the way… kids are going to love this") — written to memory (`project_writer_lab_and_tour_ideas_2026_08_19.md`); do not start it while the writer's list is open, but it is a stated product, not a musing.

Say what is wrong, fix it at the root, show a picture, one question when it is hers, never tell her to stop.
