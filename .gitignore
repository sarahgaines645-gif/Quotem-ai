# Secrets — never commit
.env
.env.*
!.env.example
.hf-secrets

# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Q's runtime data — stays on the Railway volume in production,
# stays in dev only locally. Never push memory or facts to git.
data/q-memory.json
data/q-memory-*.json
data/q-facts.json
data/q-facts-*.json
data/people.json
data/cost-log.json
data/cost-log-*.json
data/generated/
data/*.legacy

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
desktop.ini

# Build / cache
dist/
build/
.cache/
data/logo-cache/
.local-data/
.local-data.log

# ⚠️ LOCAL-ONLY CHARACTER FILES — do not redistribute.
# Mixamo's terms permit USING their characters but not redistributing the raw
# files, and the pixiv VRM is a sample whose terms we have not checked. Both
# work fine on Sarah's machine; neither belongs in the repo.
# (Microsoft Rocketbox IS committed — it is MIT licensed.)
assets/models/claire.fbx
assets/models/vrm-sample.vrm
